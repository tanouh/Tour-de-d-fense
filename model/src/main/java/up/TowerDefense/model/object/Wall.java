package up.TowerDefense.model.object;

public class Wall extends PlaceableObstacle{

    public Wall(int x, int y, int size, int lifeMax, int lifeCurrent, int cost) {
        super(x, y, size, lifeMax, lifeCurrent, cost);
        //TODO à modifier
    }
}
