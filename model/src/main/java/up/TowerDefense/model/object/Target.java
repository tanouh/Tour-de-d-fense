package up.TowerDefense.model.object;

public class Target extends DestructibleObstacle{

    public Target(int x, int y, int size, int lifeMax, int lifeCurrent) {
        super(x, y, size, lifeMax, lifeCurrent);
        //TODO à modifier
    }
}
